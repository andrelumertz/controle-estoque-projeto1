{
    "name": "controle-estoque",
    "version": "1.0.0",
    "scripts": {
        "dev:tailwind": "npx tailwindcss -i ./style.css -o ./dist/output.css --watch",
        "dev:server": "live-server --open=./login.html",
        "dev": "concurrently \"npm:dev:tailwind\" \"npm:dev:server\""
    },
    "devDependencies": {
        "concurrently": "^9.2.1",
        "live-server": "^1.2.2",
        "tailwindcss": "^3.4.1"
    }
}
